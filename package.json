{
  "name": "telemetry-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @telemetry-desk/dashboard",
    "build": "npm run build -w @telemetry-desk/domain && npm run build -w @telemetry-desk/shared && npm run build -w @telemetry-desk/application && npm run build -w @telemetry-desk/platform && npm run build -w @telemetry-desk/infrastructure && npm run build -w @telemetry-desk/dashboard && npm run build -w @telemetry-desk/desktop",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
